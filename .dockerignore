# Build context for Dockerfile.vercel. Without this the whole working tree is
# uploaded and sent to the daemon — node_modules alone is hundreds of
# megabytes, and it is reinstalled inside the image by `npm ci` anyway.
node_modules
.next
.git
.github
tests
.env*
!.env.example
*.tsbuildinfo
npm-debug.log*
.DS_Store
