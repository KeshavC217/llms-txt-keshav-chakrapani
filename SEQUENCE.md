1. Built the llms.txt from the single fetched page only — `lib/llmsTxt.ts` pulls the title/description, the same-site links as subpages, and the page's own text, and `/api/generate` now returns that rendered template instead of the raw body.
2. Made the GitHub repo public after checking the full history for committed secrets (only `.env.example` was ever tracked, no secret-shaped strings), which also unlocks branch protection on the free plan.
3. Wired up merge → deploy: added a lint/typecheck CI workflow, protected `main` behind a passing `ci` check, and shipped the generator through PR #1 — fixing a pre-existing `npm ci` lockfile break along the way — then confirmed the merge deployed to production.
4. Defined `TEMPLATE.txt` — the target llms.txt skeleton, generalized from the llmstxt.org spec plus a 22-file sample from llmstxt.site, fixing the structure and slot rules while leaving section vocabulary site-derived.
5. Built a naive extractor against TEMPLATE.txt — a small HTML tree (`lib/dom.ts`), text utilities (`lib/nlp.ts`) and `lib/naiveExtractor.ts`, which uses link density to drop boilerplate, path segments plus nav headings to name sections, token overlap to dedupe titles and reject notes that restate their link, and caps to keep the file curated.
6. Made spec conformance enforced rather than claimed — `lib/spec.ts` implements the llmstxt.org grammar in both directions (escaping on output, parsing to verify), every response reports validity, and `npm run conformance` runs the spec's own example, six must-reject files and eight hostile pages in CI.
7. Added a zero-dependency unit suite on node:test (59 tests over nlp, dom, spec and the extractor, with mock pages per site genre), which found and fixed five real defects including an implicit-close table keyed backwards in the HTML parser.
8. Integrated Supabase email/password auth — session refresh in `proxy.ts` (Next 16's replacement for middleware), a verified `getUser()` check, and `lib/authGate.ts` gating the LLM path so the generator stays open to everyone while AI features require an account.
9. Fixed a false "application shell" claim — client rendering now needs positive evidence (a script plus an empty mount element) rather than being inferred from the absence of links, which was equally true of small complete pages like example.com.
10. Added the AI sieve — a guide pass for the summary and section names, chunked annotation for per-link notes, and acceptance filters that drop anything a model invents, with the deterministic file served whenever the result would be worse or invalid.
11. Told model failures apart — retry with jittered backoff on 429s and 5xx (honouring Retry-After), stop immediately on 402/401 and answer 503, and report failures by kind, which surfaced a deadline truncating chunks and led to a wider budget.
12. Added an end-to-end evaluation that samples sites publishing their own llms.txt, generates both our outputs and has a cheap model grade each against the published file — showing the AI sieve gains on descriptions and structure while coverage stays at the noise floor, as it must.
13. Taught the fetcher to tell refusals apart — a browser-identity retry when a site merely dislikes our agent (zillow: 403 to 99 links), explicit detection of anti-bot challenges instead of generating a file about Cloudflare, and an optional remote-browser render for pages whose links only exist after JavaScript.
13b. Removed the browser-identity retry after failing to validate it — zillow's 403 turned out to track HTTP client and address reputation rather than the User-Agent, so the swap was shipping an impersonation with no demonstrated benefit.
14. Stored AI-assisted generations globally in Supabase behind the secret key — the enhance endpoint serves a fresh one without touching the site or the models (28.8s to 0.6s), and degrades to plain generation when the table or key is absent.
15. Added a crawler — the site's own llms.txt when it publishes one, otherwise sitemap and link discovery paced by what the site tolerates, honouring robots.txt, with each page's own title and description replacing names guessed from slugs.
16. Made the crawl deterministic — pages are planned before any fetch and results sorted back into plan order, so the same site yields byte-identical output and a content hash can mean the site changed rather than that a packet was slow.
17. Added the updater — a scheduled re-check that compares a model-free fingerprint of the site, escalating from sitemap to crawl to regeneration only as far as needed, with each site's interval following how often it actually changes.
18. Raised monitoring throughput — sites are checked four at a time rather than one, which took thirteen checks from 46 seconds to 11, and the schedule moved to every five minutes offset off the hour, where GitHub says load is highest.
19. Moved the queue loop into the GitHub runner, which has six hours where a function has sixty seconds, while leaving the crawling on the deployment so no credentials reach CI and requests keep the app's own address rather than a shared one.
20. Simplified the model — one endpoint that always crawls, always runs the models and always saves, generating behind an account while reading stays open to anyone, and the AI toggle and the four conditions on saving are gone.
21. Saved the site's own llms.txt too, labelled as theirs rather than ours, and taught the scheduled check to re-read a published file instead of crawling and replacing their curation with our generated version.
22. Made the documentation describe the program that exists — the README still said the tool fetched one page and did not crawl, documented an `/api/enhance` removed two PRs earlier, and pointed at a tag as the "full implementation"; added the schema as `db/schema.sql` and the automated-updates story the README had never told, re-measured the output against published files now that there is one endpoint to grade rather than two (8.69/15, judge noise 0.63), repaired `npm run integration` which had been calling the deleted endpoint, and deleted three pieces of dead code the prose was still describing as live.
23. Tested the routes as functions — a resolve hook so Node can import a handler at all (the `@/` alias is a tsconfig `paths` entry, and `next/server` is a package with no exports map), the refresh loop's budget arithmetic lifted out of a closure into `RunBudget` so it could be exercised, and twenty tests over the auth gate's ordering, both short circuits, the published-file labelling and the cron comparison — each checked by breaking the code it covers.
24. Put one clock on the request — every step had its own timeout and nothing bounded the sum, so a slow site ran past the function's sixty seconds and was killed, returning nothing, storing nothing, and leaving the next attempt to die identically; `lib/deadline.ts` is started when the request arrives and each step now takes the shorter of its own cap and what remains. The crawl's overrun turned out to be the smaller half: news.ycombinator.com asks for a ten-second crawl delay, the pacer's queue is shared between four workers, and `wait()` slept through it twice per page without consulting any budget — 71.2s to 28.4s, and a file with 23 notes where there had been a timeout.
25. Cut what the crawler had made redundant — `lib/grouping.ts` had been extracted from the extractor and the original never switched over, so `chooseDepth`, `groupKeyFor` and `curate` existed twice; unifying them turned out to improve the output as well as shorten it, since grouping's version orders links carrying a note ahead of those without and Wikipedia's bare-number links (`1547`, `1961`) gave way to real articles. Also deleted the headless-browser hook, which had never executed in any environment and which the project's own measurements showed would not fix the case it was aimed at, and the schema fallback in the store, which defended a migration gap that `db/schema.sql` now closes.
27. Made one link mean one page — `airbnb.com` was listing its gift-card page ten times under `?card_name=`, and a whole section of the file was one page under ten spellings. The query cannot simply be dropped, since `en.wikipedia.org` addresses every article as `/w/index.php?title=X`, so the title decides: same address and same page-name is one link, same address and different names is several, with titles matched by containment because the same page arrives as "Connections" from a link and as its full `<title>` from the page. Also stopped stripping `index.php` from a URL that has a query, which had been inventing addresses we never fetched; dropped the thirteen Wikipedia links that were operations on a page rather than pages; unified the third copy of the canonicaliser onto `lib/crawl/url.ts`; and deleted `change_count` and `markdownAlternate`, which were written and returned and read by nobody.
28. Read pages the way a browser would — `resy.com` and `docs.convex.dev` return shells with no links and embed nothing a parser could use, so Chromium now runs in the function when a fetch finds nothing. Two things had to be got right: waiting for `load` rather than `networkidle`, since resy holds connections open and idle never arrives (it timed out at thirty seconds producing nothing, where `load` gave 222 links); and refusing a title the site serves for every page, because the first working render made the file worse — every resy address returns the same shell titled "Right This Way" and the crawl wrote it over all forty-one link names. Convex is fixed everywhere; resy is fixed on a laptop and not on the deployment, and is recorded as an unexplained gap rather than a solved case.
29. Ranked the pages rather than taking them alphabetically — how often a site links to a page decides which of its candidates are crawled, since that is the site voting on what matters and it costs nothing to count (react.dev links `/learn` five times and `/reference/react` and `/blog` four, which are its three most important pages). Deterministic on purpose: a model in the selection path would make two runs of an unchanged site pick different pages and rewrite the file forever. Sections now get budget in proportion to their size with a floor of one, so two hundred documentation pages and two careers pages are no longer treated as equally important. The signal grows as the crawl proceeds — modal.com goes from 7 pages linked more than once to 171 by the second wave — and it is where rendering and ranking compose, since a shell like docs.convex.dev has no home-page links to count until a browser has run.
30. Made CI gate the deploy rather than only the merge — branch protection stops a red pull request merging, but Vercel builds on every push to main and starts the moment the push lands, in parallel with that commit's CI run rather than after it, so a merge that goes red on main or a direct push reached production unchallenged. `scripts/gate-deploy.mjs` runs as Vercel's Ignored Build Step, waits for the `ci` check on the exact commit, and skips the build unless it passed; it needs no credentials because the repository is public. Anything it cannot resolve fails closed, since deploying because we could not find out whether the tests passed would make the gate decoration.
31. Narrated the wait instead of asking the caller to sit through it in silence — /api/generate streams one JSON object per line as it fetches, maybe renders, crawls and runs the two model passes, rather than the caller getting one opaque await for up to fifty seconds. The request stays synchronous; background crawling was built in full once already and closed unmerged because fifty pages fits the fifty-second budget, so this narrates the same single function rather than reopening that design. Every stage that has a count reports it — crawling as fetched/planned, annotating as completed/total — verified live against modal.com and docs.convex.dev rather than assumed: real per-page and per-chunk timestamps arriving over an actual open connection, not buffered to the end. A response that needs no real work — a saved file, a validation error — stays a single application/json object exactly as before; only the response that commits to real work switches to application/x-ndjson, told apart by content-type so nothing about the fast paths changed.
