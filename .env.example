# --- LLM copyedit pass (optional; the generator works fully without it) ---
# Get a key at https://openrouter.ai/settings/keys
OPENROUTER_API_KEY=
# Override the default model (google/gemma-4-31b-it)
# OPENROUTER_MODEL=

# --- Supabase (required for monitoring / automated updates) ---
# Project settings -> Data API -> Project URL
SUPABASE_URL=
# Project settings -> API keys -> service_role. SERVER ONLY, never NEXT_PUBLIC_.
# Tracked sites are anonymous and all access is server-side, so no publishable
# key is needed: the browser never talks to Supabase directly.
SUPABASE_SERVICE_ROLE_KEY=

# --- Monitoring cron ---
# Shared secret required by POST /api/refresh. Generate with:
#   openssl rand -hex 32
CRON_SECRET=

# --- Crawl tuning (optional, defaults shown) ---
# MAX_CRAWL_PAGES=100
# MAX_CRAWL_CONCURRENCY=8
